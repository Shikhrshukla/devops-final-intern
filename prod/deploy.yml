apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mydp1
  name: mydp1
spec:
  replicas: 4
  selector:
    matchLabels:
      app: mydp1
  strategy: {}
  template:
    metadata:
      labels:
        app: mydp1
    spec:
      containers:
      - image: shikhrshukla/devops:apache-webserver-php-2
        name: php-app
        resources: {}
